import React, {Component} from 'react';
import {render} from 'react-dom';
import './content-title.css';

export class ContentTitle extends Component {
    render() {
        return (
            <h2 className="content-title">Offices</h2>
        )
    }
}
